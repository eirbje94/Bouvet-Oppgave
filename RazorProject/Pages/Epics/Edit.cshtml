@page
@model RazorProject.Pages.Epics.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Epic</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Epic.EpicId" />
            <div class="form-group">
                <label asp-for="Epic.EpicName" class="control-label"></label>
                <input asp-for="Epic.EpicName" class="form-control" />
                <span asp-validation-for="Epic.EpicName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Epic.Description" class="control-label"></label>
                <input asp-for="Epic.Description" class="form-control" />
                <span asp-validation-for="Epic.Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Epic.Project.ProjectName" class="control-label"></label>
                <select asp-for="Epic.ProjectId" class="form-select" asp-items="ViewBag.ProjectId"></select>
                <span asp-validation-for="Epic.ProjectId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
